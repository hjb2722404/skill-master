---
name: meta-cognitive-skill-system
description: 一套三层元认知技能体系，用于指导Agent生成和迭代各领域的方法论。当需要进入新领域、为现有领域构建方法论、或优化现有技能时激活此技能。
license: MIT
metadata:
  author: skill-system-designer
  version: "1.0.0"
  layer-count: "3"
  l1-count: "6"
---

# 元认知技能体系（Meta-Cognitive Skill System）

## 一、这是什么

这是一套分层的元认知技能体系，帮助Agent系统性地构建、评估和迭代各领域的方法论。

### 三层架构

| 层级 | 名称 | 定位 | 生成方式 | 存放位置 |
|------|------|------|----------|----------|
| L1 | 元元技能 | 顶层认知原则与方法论生成指南 | 静态文件 | `meta-skills/` |
| L2 | 元技能 | 特定领域的方法论 | 动态生成 | 可配置（默认 `generated-meta/`） |
| L3 | 普通技能 | 具体场景的执行技能 | 动态生成 | 可配置（默认 `generated-skills/`） |

### 核心设计思想

- **L1 不直接参与日常任务** —— 它们只在生成/修订 L2 时被调用
- **L2 是领域方法论** —— 回答"在这个领域应该怎么思考和行动"
- **L3 是具体技能** —— 回答"在这个场景下具体怎么做"

---

## 二、什么时候使用

### 必须激活的场景

1. **进入新领域** —— 需要为该领域生成 L2 方法论
2. **现有方法论不足** —— 执行任务时发现 L2 有缺陷，需要修订
3. **方法论迭代** —— 定期复盘后需要升级 L2
4. **生成具体技能** —— 基于 L2 为特定场景生成 L3

### 不使用的场景

- 已有完善 L2 和 L3 的常规任务 —— 直接使用对应的 L3 技能
- 简单、明确的单步任务 —— 不需要动用整个体系

---

## 三、L1 元元技能清单

### 3.1 核心元元技能（串行调用）

按以下顺序串行调用：

| 顺序 | ID | 名称 | 文件 | 核心作用 |
|------|-----|------|------|----------|
| 1 | mm-001 | 认识论框架 | `meta-skills/epistemology.md` | 定义"怎么判断对错" —— 建立领域的认知标准 |
| 2 | mm-002 | 分解与抽象 | `meta-skills/decomposition.md` | 定义"怎么拆解问题" —— 建立领域的结构视图 |
| 3 | mm-003 | 因果与系统思维 | `meta-skills/causal-thinking.md` | 定义"怎么理解因果" —— 建立领域的动态模型 |
| 4 | mm-004 | 元认知与自我反思 | `meta-skills/meta-cognition.md` | 定义"怎么审视自己" —— 建立质量检查机制 |
| 5 | mm-005 | 价值与优先级框架 | `meta-skills/value-priority.md` | 定义"什么更重要" —— 建立决策排序标准 |

### 3.2 横向元元技能（并行调用）

| ID | 名称 | 文件 | 核心作用 |
|-----|------|------|----------|
| mm-006 | 交互与沟通元模型 | `meta-skills/interaction.md` | 定义"怎么与人交互" —— 确保方法论的用户接口质量 |

**调用方式**：可与5个核心元元技能并行调用，或在它们之后调用

---

## 四、使用流程

### 4.1 生成 L2 方法论（完整流程）

```
开始
  │
  ▼
加载 L1 元元技能
  │
  ├──→ [串行] 认识论框架
  │       │
  │       ├──→ 【强制】网络搜索权威资料（mm-001 3.0）
  │       │       ├──→ 搜索学术论文
  │       │       ├──→ 搜索行业标准/规范
  │       │       ├──→ 搜索权威书籍/经典著作
  │       │       └──→ 搜索领域专家观点
  │       │
  │       └──→ 基于搜索资料建立认知标准（mm-001 4.1-4.6）
  │               └── 输出：领域知识来源、证据等级、认知陷阱
  │
  ├──→ [串行] 分解与抽象
  │       └── 输出：领域实体、分解维度、抽象层级
  │
  ├──→ [串行] 因果与系统思维
  │       └── 输出：因果链、反馈回路、二阶效应
  │
  ├──→ [串行] 元认知与自我反思
  │       └── 输出：偏差防范、自检点、复盘模板
  │
  ├──→ [串行] 价值与优先级框架
  │       └── 输出：价值排序、风险分级、合格标准
  │
  └──→ [并行] 交互与沟通元模型
          └── 输出：用户画像、意图还原、沟通策略
  │
  ▼
整合生成 L2 方法论（必须符合L2输出格式规范）
  │
  ▼
质量审核（调用各 L1 的审核标准，包括E-00搜索完成检查）
  │
  ▼
输出为 SKILL.md 到 L2 存放目录
  │
  ▼
结束
```

**重要**：认识论框架 (mm-001) 现在包含强制搜索步骤 (3.0)，这是生成L2的**前提条件**。

### 4.2 修订 L2 方法论

当执行中发现 L2 存在问题时：

1. **问题定位** —— 确定是哪个 L1 维度出了问题
2. **选择性重生成** —— 只调用相关的 L1，不必完整流程
3. **增量更新** —— 修订受影响的部分，保持其他部分稳定

### 4.3 生成 L3 普通技能

基于已验证的 L2：

1. 确定具体场景和任务
2. 从 L2 中提取相关原则
3. 添加具体步骤、示例、注意事项
4. 输出到 L3 存放目录

---

## 五、L2 输出格式规范

### 5.1 核心原则

**L2 方法论本身必须是一个符合 Agent Skill 规范的文档**，因为它是一个"用来生成技能的技能"。

### 5.2 L2 职责边界（重要约束）

**L2 只能用于生成 L3 技能，不能直接介入具体场景。**

| 层级 | 正确职责 | 错误职责 |
|------|----------|----------|
| **L2 方法论** | 生成 L3 技能 | ❌ 直接做技术选型决策 |
| | 提供生成 L3 的原则 | ❌ 直接审查具体代码 |
| | 回答"如何生成技能" | ❌ 直接解决具体问题 |
| **L3 技能** | 执行具体任务 | ✅ 选择状态管理方案 |
| | 解决具体问题 | ✅ 审查代码 |
| | 回答"具体怎么做" | ✅ 分析性能瓶颈 |

**激活条件约束**：
- ✅ **正确**："需要生成React表单处理技能"、"缺少前端代码审查技能"
- ❌ **错误**："选择状态管理方案"、"审查这段代码"（这些是 L3 的职责）

**输出约束**：
- L2 的输出必须是**一个 L3 技能文档**，不能直接是决策结果或解决方案

### 5.3 文件规范

| 规范项 | 要求 |
|--------|------|
| **文件名** | 必须是 `SKILL.md` |
| **存放路径** | `[l2-output-dir]/[domain-name]/SKILL.md` |
| **frontmatter** | 必须包含标准字段（name, description, metadata） |

### 5.4 Frontmatter 规范

```yaml
---
name: [domain]-methodology              # 小写字母、连字符
description: >                          # 描述何时使用此技能
  本技能用于指导Agent在[领域]中生成具体技能。
  当需要为[领域]生成L3技能、或需要[领域]的决策指导时激活此技能。
license: MIT
metadata:
  author: meta-cognitive-skill-system
  version: "1.0.0"
  layer: "L2"
  domain: [domain-name]
  generated_from:                         # 记录生成来源
    - mm-001 认识论框架
    - mm-002 分解与抽象
    - mm-003 因果与系统思维
    - mm-004 元认知与自我反思
    - mm-005 价值与优先级框架
    - mm-006 交互与沟通元模型
---
```

### 5.5 正文结构规范

L2 SKILL.md 必须包含以下章节：

| 章节 | 内容 | 目的 |
|------|------|------|
| **一、概述** | 这是什么、适用范围、核心原则 | 让Agent快速判断是否适用 |
| **二、何时使用** | 激活条件（**仅限生成L3**）、输入、输出 | 明确使用场景 |
| **三、核心原则** | 该领域的核心认知原则 | 指导L3生成 |
| **四、生成L3技能指南** | 如何基于本方法论生成具体技能 | 核心使用指南 |
| **五、L3技能清单** | 本方法论可生成的L3技能列表 | 参考 |
| **六、质量审核** | L2自身的质量审核结果 | 可信度标注 |
| **七、版本历史** | 变更记录 | 追溯性 |

### 5.6 "何时使用"章节约束

**必须明确声明**：
- 本技能**只能**用于生成L3技能
- **不能**用于直接解决具体问题或做决策
- 具体问题的处理应该由生成的L3技能完成

**正确的激活条件示例**：
- "需要生成[具体技能名称]技能"
- "缺少[领域]的[场景]技能"
- "需要为[场景]创建执行技能"

### 5.7 "生成L3技能指南"章节要求

这是 L2 作为"技能生成技能"的核心，必须包含：

1. **L3 生成触发条件** —— 什么情况下应该基于本方法论生成L3
2. **L3 结构模板** —— 生成的L3应该包含哪些部分
3. **内容提取指南** —— 从本方法论中提取哪些原则用于L3
4. **示例** —— 至少一个从本方法论生成L3的示例

### 5.8 渐进式披露

- **元数据**（~100 tokens）：name + description，加载时可见
- **核心指令**（~500 tokens）：概述 + 何时使用 + 核心原则
- **详细参考**（按需加载）：生成指南 + 决策参考 + 质量审核

---

## 六、目录配置

### 默认结构

```
skills/
├── SKILL.md                    # 本文件
├── meta-skills/                # L1：元元技能（静态）
├── generated-meta/             # L2：元技能（动态生成）
└── generated-skills/           # L3：普通技能（动态生成）
```

### 配置方式

在生成 L2/L3 时，可通过以下方式指定存放路径：

- **环境变量**：`META_SKILL_PATH`、`STANDARD_SKILL_PATH`
- **参数传递**：生成命令中指定 `--output-dir`
- **默认回退**：使用上述默认结构

---

## 六、质量审核机制

每个 L1 都定义了对应的 L2 质量审核标准：

| 元元技能 | 必须通过项数 | 推荐通过项数 |
|----------|--------------|--------------|
| 认识论框架 | 7 | 3 |
| 分解与抽象 | 5 | 2 |
| 因果与系统思维 | 5 | 2 |
| 元认知与自我反思 | 5 | 2 |
| 价值与优先级框架 | 5 | 2 |
| 交互与沟通元模型 | 5 | 2 |

**审核结果标签**：
- `合规·优秀` —— 全部必须项 + 足够推荐项通过
- `合规·合格` —— 全部必须项通过，推荐项不足
- `不合规·需修订` —— 任一必须项不通过

---

## 七、版本管理

### L1 版本策略

- **不可变原则**：L1 一旦发布，内容不再修改
- **版本号格式**：`主版本.次版本.修订号`
- **升级方式**：创建新文件（如 `epistemology-v2.md`），保留旧版本

### L2/L3 版本策略

- 每次生成/修订都更新版本号
- 保留历史版本（可选，取决于存储策略）
- 在文件中记录变更历史

---

## 八、与其他技能的关系

### 本体系的定位

这是一个**技能生成技能**（meta-skill），它本身不解决具体领域问题，而是帮助生成解决领域问题的技能。

### 使用边界

- ✅ 适合：需要系统性方法论支持的复杂领域
- ❌ 不适合：已有成熟方案、或简单到不需要方法论的任务

---

## 九、快速参考

### 生成 L2 的触发条件

| 场景 | 操作 |
|------|------|
| 进入全新领域 | 完整流程，全部 L1 |
| 现有 L2 部分失效 | 定位问题，调用相关 L1 |
| 定期迭代 | 复盘后，按需调用 L1 |

### L1 文件速查

```
meta-skills/
├── epistemology.md      # mm-001 认识论框架
├── decomposition.md     # mm-002 分解与抽象
├── causal-thinking.md   # mm-003 因果与系统思维
├── meta-cognition.md    # mm-004 元认知与自我反思
├── value-priority.md    # mm-005 价值与优先级框架
└── interaction.md       # mm-006 交互与沟通元模型
```

---

## 十、版本历史

| 版本 | 日期 | 变更说明 |
|------|------|----------|
| 1.0.0 | 2026-04-09 | 初始版本，建立三层架构和6个L1元元技能 |

---

*本技能体系遵循渐进式披露原则：此文档提供体系概览，具体实现细节请参阅各 L1 文件。*
